#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <limits.h>
#include "header_ex3.h"


int main(int argc, char** argv) {
        int max=INT_MIN;
        int min=INT_MAX;
        Saisi();
        max=Max(nombre);
        min=Min(nombre);
        printf("Maximum : %d\nMinimum : %d",max,min);

	getch();
}

 
