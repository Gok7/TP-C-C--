//---------------------------------------------------------------------------
#include "Unit2.h"
#include <vcl.h>
#pragma hdrstop

//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma resource "*.dfm"
TIP_Destinataire *IP_Destinataire;
//---------------------------------------------------------------------------
__fastcall TIP_Destinataire::TIP_Destinataire(TComponent* Owner):TForm(Owner)

{
}
//---------------------------------------------------------------------------



void __fastcall TIP_Destinataire::OnKeyDown_IP_Destinataire(TObject *Sender, WORD &Key,
      TShiftState Shift)
{
        char IP[14];
        char Port[6];
        if(Key==VK_RETURN){

              // creation_info_envoi(const char* P_Adresse, int P_port)

              IP_Destinataire->Close();
        }
}








